apiVersion: batch/v1
kind: Job
metadata:
  name: wget-test
spec:
  backoffLimit: 3
  activeDeadlineSeconds: 30
  template:
    spec:
      containers:
        - name: wget-test
          image: busybox
          command: ["wget", "--timeout=3", "http://{test-address}"]
      restartPolicy: Never
